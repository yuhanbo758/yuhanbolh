> yuhanbolh 导出说明：qmt_bridge_server.py 已转换为 GBK，可作为大QMT部署副本；其余文件为 UTF-8。修改服务端请保持 GBK。此包不附任何实际运行验收报告。

# 独立桥接验证说明

本模板不携带原项目测试结果。请在导出副本执行以下命令，以当前源码生成自己的验证报告。新解释器的Python 3.6 AST语法检查不能替代实际大QMT内置解释器运行。

## 复现命令

在本目录执行：

```powershell
python -B -m unittest discover -s . -p 'test_*.py' -v
python -B .\validate_source.py
```

在项目根目录执行时，测试目录改为 `-s .\qmt_bridge_standard`。外部示例使用example_前缀且有main守卫，unittest不会执行它们的网络入口。

独立性验证将本目录的Python与Markdown文件复制到全新临时目录，将工作目录和测试导入路径指向该副本后运行同一测试发现命令，不提供父项目模块。分别用外部Python和大QMT安装目录内的Python3.6解释器运行。测试报告仅含计数和相对文件名哈希，不包含临时目录绝对路径。

## 测试覆盖

- 原生行情参数完整传递、历史fill_data/subscribe、缺失数据、JSON非有限数处理和QMT回调线程亲和性。
- 双账户关闭、单账户路由、普通/信用操作码、价格数量和策略标识、真实开关及原生交易日历门禁。
- dry-run不连接数据库、不写路由、不报单；HTTP产品验证、令牌、方法限制、POST不自动重试。
- 持久化幂等、同请求号参数冲突、结果未知、重启、排队过期、执行中超时、防止嵌套回调重复排空。
- 部分/重复/冲突成交、超量成交、账号/日期/成交编号组合去重、委托已成不冒充成交、撤单后迟到成交。
- 首次绑定需要桥接备注，已绑定空备注唯一匹配，歧义/缺字段拒绝；官方异常下单字段；同步回调先于passorder返回。
- SQLite故障事务回滚、恢复重试、柜台查询恢复漏回报及来源标记、HTTP查询保持只读。
- 旧结构、缺列/多列、缺少或错误唯一索引、其他应用数据库拒绝，拒绝前后数据库字节不变。
- 长轮询唤醒、事件过滤、游标缺口、重启标识、SSE心跳/元信息、多行数据与不完整帧。
- HTTP响应头/正文/OPTIONS/SSE连接中止不追加第二响应，绑定失败重试、并发启动、停止再启动、线程启动失败关闭套接字。
- init阶段注册历史起点定时器、真实定时心跳与handlebar区分、客户端拒绝已知停滞的RPC；运行检查器不把空回推流当成交成功。

## 编码与部署工具

`validate_source.py` 递归检查源码目录全部Python文件，普通文件按UTF-8读取，服务端同时支持GBK部署字节；在内存规范编码声明后compile，不执行被检代码。新解释器使用可用的AST Python3.6语法模式，实际3.6执行原生compile；服务端另做GBK字节编译。

`--report-dir` 可指定已有报告目录。`--deploy-output` 只有显式指定且静态校验通过才生成GBK副本，目标存在即拒绝。普通校验不生成部署副本。export_qmt_bridge已经完成服务端GBK转换，通常无需再次生成部署副本。

## 尚未验证的实际环境

实际大QMT加载后仍须核对1693健康接口的产品、源码版本、能力与运行状态。原生行情覆盖、券商信用操作资格、实际委托、成交及回推延迟尚未验收。真实订单示例要求双确认和服务端许可；dry-run只做参数预演，资金、持仓、资产资格及数量单位仍需调用方核对，最终由柜台处理。

定时对账只恢复能够通过严格路由识别且仍在柜台查询范围内的本桥接记录；不明来源、歧义和历史已不可查询的记录需要人工核对，不会自动重报。
