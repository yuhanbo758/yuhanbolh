# 本次迁移验收记录

验证日期：2026-09-06。分支：codex/wencai-akshare-qmt-bridge。未提交、推送或发布；构建版本仍沿用源码0.6.4，不表示本次变更已在PyPI上线。

## 已通过

| 验证 | 结果 |
| --- | --- |
| Python 3.11.15隔离环境 | 24项库级测试通过；AKShare 1.18.94、pandas 3.0.5 |
| Python 3.12.9隔离环境 | 24项库级测试通过 |
| 从安装后的wheel导出独立桥接 | 14个文件齐全，不依赖原项目或Skill目录 |
| 导出副本隔离测试 | 75项通过，假ContextInfo、模拟报单、临时数据库，无真实账户 |
| 导出副本静态校验 | 9个Python文件通过；Python 3.6 AST语法、GBK往返和字节编译通过 |
| 发行包 | wheel/sdist构建、Twine检查通过 |
| 缺少终端/旧问财依赖时导入 | 未安装pywencai、xtquant、MetaTrader5，问财和桥接公开入口可用 |
| 静态检查 | 新模块/测试Ruff检查、库源码AST检查、git diff --check通过 |
| 文档一致性 | 30篇存在，9篇原有文档元数据保留，旧调用仅在迁移说明中出现 |

75项桥接测试由库级测试调用；这里分别列出层级，不把它们当成独立的真实运行验收。

## 公开数据检查

- akshare_convertible_bond：成功返回314条记录、18列，确认强赎状态和强赎天计数分列。
- stock_zh_a_spot_em、bond_cov_comparison：均未成功取得实际数据；进一步请求诊断均为ConnectionError。字段和分页行为已离线测试，真实取数可用性仍待网络恢复后验证。
- 问财：仅模拟HTTP验证协议、分页与字段；本次未使用用户凭据调用OpenAPI，未验证实际配额与各市场查询权限。

## 未执行

未在大QMT原生Python 3.6解释器运行；AST语法兼容不等于原生运行验收。未启动/重载QMT、未访问真实账户或生产数据库、未提交真实委托，也未验证实际柜台成交/回推/账本链路。

## 本地文档

新增21篇、修改9篇、未删除文档。逐篇清单见 [文档变更清单](documentation-changes.md)。用户需自行同步本地知识库文档到远端。
